Author: IOhannes m zmölnig
Description: make OSS MIDI available as fallback for ALSA MIDI
--- puredata.orig/src/s_midi.c
+++ puredata/src/s_midi.c
@@ -501,7 +501,9 @@
 {
     int n = 0;
     strcpy(buf, "{ ");
+#ifdef USEAPI_OSS
     sprintf(buf + strlen(buf), "{OSS-MIDI %d} ", API_DEFAULTMIDI); n++;
+#endif
 #ifdef USEAPI_ALSA
     sprintf(buf + strlen(buf), "{ALSA-MIDI %d} ", API_ALSA); n++;
 #endif
